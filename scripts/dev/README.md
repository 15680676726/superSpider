# Development Helpers

This directory contains ad hoc local development helpers that are useful for maintainers but should not live in the repository root.

Current contents:

- `run_codex_main_agent.bat`: local Codex audit helper for inspecting the main-brain path

These scripts are not part of the public runtime contract. They are maintainer-facing helpers.
