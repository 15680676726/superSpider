# 46问题文档

> `2026-04-06` authoritative update:
> 本文前半部保留历史审计轨迹，但其中关于 `kickoff/auto-resume`、acquisition producer/kernel identity、capability execution 前门、`goal_ids / schedule_ids / active_goal_ids`、workflow historical links、reporting/prediction goal-truth residue 的“未完成”判断已过时。当前权威状态以本文后续新增更新说明与最新验证矩阵为准。

## 1. 审计结论

本轮在前一阶段 6 个协助 agent 的分域分析基础上，又补做了源码复核、回归测试和流程核实。当前结论不是“项目没落地”，也不是“已经完全闭环”，而是：

> 当前仓库已经具备真实可跑的正式主链，但仍然存在 legacy `goal/schedule` 残留、Decision/Capability/Evidence 治理前门未完全统一、以及多处运行真相并存的问题，因此只能认定为`部分闭环`。

本轮最大的变化是：

- 一批此前已经被证实的真实回归问题已经修掉，并重新通过正式测试确认。
- 一批旧文档中的“未解决问题”已经过时，必须改写。
- 仍然没有证据支持“单一真相源、单一执行前门、单一治理主链已经全部收死”。

当前综合判断：

- 闭环程度：`部分闭环`
- 真实性：`高于上一轮`
- 当前代码主链可信度：`中上`
- 文档口径可信度：`需要持续校正`

---

## 2. 本轮正式验证

以下结果均为本轮重新执行所得：

- `PYTHONPATH=src python -m pytest tests/state/test_human_assist_task_service.py -q` -> `15 passed`
- `PYTHONPATH=src python -m pytest tests/app/test_runtime_human_assist_tasks_api.py -q` -> `14 passed`
- `PYTHONPATH=src python -m pytest tests/app/test_learning_api.py -q` -> `12 passed`
- `PYTHONPATH=src python -m pytest tests/app/test_runtime_center_api.py -q` -> `105 passed`
- `PYTHONPATH=src python -m pytest tests/app/runtime_center_api_parts/overview_governance.py -q` -> `83 passed`
- `PYTHONPATH=src python -m pytest tests/industry/test_runtime_views_split.py -q` -> `12 passed`
- `PYTHONPATH=src python -m pytest tests/app/test_phase_next_autonomy_smoke.py -q` -> `11 passed`
  - 注意：并行执行时在 `124s` 超时；串行重跑后通过，耗时 `289.59s`
- `PYTHONPATH=src python -m pytest tests/app/test_operator_runtime_e2e.py -q` -> `9 passed`
- `PYTHONPATH=src python -m pytest tests/app/test_runtime_canonical_flow_e2e.py -q` -> `4 passed`
  - 注意：并行执行时在 `124s` 超时；串行重跑后通过，耗时 `128.22s`

## 2026-04-07 follow-up closure update

- industry bootstrap `auto_dispatch` 已切到 assignment dispatch 前门，不再在 activation write-path 里使用 legacy goal dispatch 作为现役执行语义。
- workflow run detail / resume 已不再依赖持久化 `linked_schedule_ids` 才能恢复 schedule step；schedule runtime identity 现在按 run/step payload 进行 deterministic 推导。
- workflow resume 在存在 `linked_task_ids / linked_decision_ids / linked_evidence_ids` 时，不再回填 legacy `linked_goal_ids`；只有完全没有 runtime context 时才会重建 goal link 作为兜底。
- focused verification:
  - `python -m pytest tests/app/industry_api_parts/runtime_updates.py -q` -> `43 passed`
  - `python -m pytest tests/app/industry_api_parts/bootstrap_lifecycle.py -k "public_bootstrap_auto_activate_keeps_instance_active_without_legacy_goal_dispatch or public_bootstrap_auto_dispatch_materializes_assignment_tasks_without_goal_dispatch or kickoff_execution_from_chat_dispatches_bootstrap_assignments_without_goal_dispatch or run_operating_cycle_dispatches_materialized_execution_assignment or run_operating_cycle_auto_dispatch_keeps_assignment_formal_planning_in_compiled_task or run_operating_cycle_auto_dispatch_uses_assignment_checklist_instead_of_generic_steps" -q` -> `6 passed, 38 deselected`
  - `python -m pytest tests/app/test_workflow_templates_api.py -k "launch_materializes_run or run_step_detail_stays_read_only_and_service_resume_rehydrates_missing_links or step_detail_prefers_persisted_task_links_over_legacy_goal_links or resume_uses_persisted_runtime_context_without_rehydrating_legacy_links" -q` -> `4 passed, 24 deselected`
  - `python -m pytest tests/state/test_reporting_service.py -q` -> `8 passed`
  - `python -m pytest tests/app/test_predictions_api.py -k "create_list_and_detail or consume_strategy_trigger_rules_into_signals_and_recommendations or recommend_schedule_copy_points_to_fixed_sop_instead_of_workflow_templates or collects_industry_tasks_without_goal_anchor or reporting_surface_prediction_metrics" -q` -> `5 passed, 13 deselected`
  - `cmd /c npm --prefix console run test -- src/pages/Insights/presentation.test.ts` -> `1 passed`

### Residual Audit Clarification

- 以下判断当前仍然成立：
  - `industry` 域仍未完全纯化；残留主要集中在 bootstrap seed、统计视图、cleanup/archive、历史桥接，而不是 runtime kickoff / auto-resume 前门。
  - reporting / prediction 现在已经是 task-first / industry-first / focus-first，但仍保留从 task 派生 goal 语义的次级分析链。
  - workflow 仍保留 `linked_goal_ids / linked_schedule_ids` 这类历史关联语言；它们更接近审计/钻取引用，而不是当前 live main-chain truth。
  - `runtime_service_graph.py` 与 `runtime_bootstrap_models.py` 组成的手工 wiring graph 仍然偏重。
  - 当前仓库仍应被定义为“部分闭环”。
- 以下判断已经过时，不应继续复述：
  - `README.md` 仍在强调 `goal-first execution`。
  - `service_activation.py` 仍通过 legacy goal dispatch 驱动 bootstrap `auto_dispatch`。
  - workflow schedule step 仍必须依赖持久化 `linked_schedule_ids` 才能恢复。
  - workflow resume 会无条件回填 legacy `linked_goal_ids`。
- 读取本文时，关于“当前还剩什么问题”的结论，应优先以后续权威更新段和本节为准，而不是直接引用前半部历史审计表述。

### Follow-up: Public Surface And Prediction Query Cleanup

- prediction task collection 现在只在缺少 primary task anchor 时才会退回 goal 语义；当 industry task truth 已存在时，不再补跑 `goal_ids` task query。
- workflow run / step 的公开 payload 已隐藏 `linked_goal_ids / linked_schedule_ids`；历史 link 仍保留在内部模型中供 step drill-down / compatibility 使用，但不再作为前台默认语义。
- focused verification:
  - `python -m pytest tests/app/test_predictions_api.py::test_prediction_service_does_not_fallback_to_goal_id_queries_when_industry_tasks_exist -q` -> `1 passed`
  - `python -m pytest tests/app/test_workflow_templates_api.py::test_workflow_run_public_surface_hides_historical_goal_schedule_id_fields -q` -> `1 passed`
  - `python -m pytest tests/app/test_workflow_templates_api.py -k "run_step_detail_stays_read_only_and_service_resume_rehydrates_missing_links or step_detail_prefers_persisted_task_links_over_legacy_goal_links or resume_uses_persisted_runtime_context_without_rehydrating_legacy_links" -q` -> `3 passed, 26 deselected`
- `PYTHONPATH=src python -m pytest tests/app/industry_api_parts/bootstrap_lifecycle.py -q` -> `38 passed`
  - 本轮最终全量通过，耗时 `572.97s`
- `python -m pytest tests/app/test_runtime_center_external_runtime_api.py -q` -> `4 passed`
- `python -m pytest tests/app/test_capabilities_execution.py -k "external_runtime or runtime_center" -q` -> `1 passed, 49 deselected`
- `python -m pytest tests/app/test_industry_service_wiring.py::test_kickoff_execution_from_chat_records_trigger_message_context_in_assignment_dispatch tests/app/industry_api_parts/bootstrap_lifecycle.py::test_kickoff_execution_from_chat_dispatches_bootstrap_assignments_without_goal_dispatch tests/app/industry_api_parts/kickoff_fixed_sop.py::test_industry_chat_kickoff_executes_in_background_without_blocking_response tests/app/industry_api_parts/kickoff_fixed_sop.py::test_industry_chat_kickoff_background_reuses_team_projection_instead_of_rematerializing_it_per_goal tests/app/industry_api_parts/retirement_chain.py::test_industry_chat_kickoff_executes_in_background_without_blocking_response tests/app/industry_api_parts/retirement_chain.py::test_industry_bootstrap_auto_activate_enters_live_coordinating_contract tests/app/industry_api_parts/retirement_chain.py::test_industry_runtime_main_chain_exposes_live_assignment_chain_after_auto_activate -q` -> `7 passed`
- `npm --prefix console run test -- src/pages/RuntimeCenter/useRuntimeCenter.test.ts src/routes/resolveSelectedKey.test.ts src/layouts/Sidebar.test.tsx src/pages/Settings/System/index.test.tsx` -> `19 passed`
- `npm --prefix console run build` -> `通过`

这意味着：

- 这轮修复后的关键后端主链已经重新拿到正式回归证据。
- `bootstrap_lifecycle` 不再停留在 focused slice，本轮最终已经全量通过。

### Follow-up: Industry / Reporting / Workflow Residual Tightening

- `industry` 状态与删除链继续收口：
  - `reconcile_instance_status()` 现在不再依赖 legacy goal 状态来把“没有 live cycle/backlog/assignment/report/schedule 的实例”判成完成态；静态 team membership 本身也不再被误算成 live execution。
  - instance delete / retire 的 task 收集现在优先按 `industry_instance_id` 归集，并把 goal-linked leaf task 并入同一 task set；execution-core 自有 task 即使没有 `goal_id` 也能被 delete cleanup 正式收走。
- reporting / prediction 继续去 goal 次级前门：
  - prediction task collection 已删除 `goal_ids` query fallback，当前只走 `industry_instance_id -> owner_agent_id -> agent_ids` 三层 task truth。
  - reporting task-scoped learning projection 不再因为“同 goal”就把不在当前 task scope 内的 proposal 混进 report。
- workflow 继续去历史 link 真相：
  - run detail / resume 的 goal step 现在优先从 `GoalOverride.compiler_context(workflow_run_id, workflow_step_id)` 反查 goal link，不再把持久化 `linked_goal_ids` 当正式真相。
  - schedule step 已继续只按 deterministic schedule identity 推导，不再依赖历史 `linked_schedule_ids`。
- `2026-04-07` 后续硬切继续完成：
  - `OperatingCycleService.reconcile_cycle(...)` 已删除 `goal_statuses` 输入，cycle status 现在只按 assignment/report truth 判定。
  - `PredictionService.create_cycle_case(...)` 已不再把 `goal_statuses` 写进 cycle fingerprint 或 case metadata。
  - `WorkflowStepExecutionRecord` 已删除内部 `linked_goal_ids / linked_schedule_ids` 字段；step detail 改为按 override/runtime context 在读时推导，不再靠 step record 持有 legacy link。
- 当前仍然真实存在、还没有在这轮物理删净的残留只剩：
  - workflow `step_execution_seed` 仍允许保留 `linked_goal_ids / linked_schedule_ids` 作为旧 run 兼容回填输入，但公开 step record/detail 已不再把它们当正式对象。
  - `runtime_service_graph.py` / `runtime_bootstrap_models.py` 的手工 wiring graph 仍偏重。
- 前端当前至少能 build，且 `Runtime Center` 主读面的关键 Vitest 已通过。

---

## 3. 本轮已修复并核实的问题

### 3.1 `HumanAssistTask` 验收合同已从“伪强语义”修成正式分模式验证

当前状态：`已修复`

本轮已确认：

- `verify_task()` 在直接观察到宿主输入时，会先物化真实 `submitted` 快照，再进入 `verifying`。
- `evidence_verified` 不再允许只靠文本锚点通过，必须有正式 `verification_evidence_refs`。
- `state_change_verified` 不再只靠文本过验，必须满足配置中的状态合同路径。
- `verification_evidence_refs` 会正式写回结果。
- evidence ledger 写入失败不再静默接受；现在会回退到验证前状态，并返回 `verification_record_failed`。

关键落点：

- `src/copaw/state/human_assist_task_service.py`
- `tests/state/test_human_assist_task_service.py`
- `tests/app/test_runtime_human_assist_tasks_api.py`

真实性结论：

- 这一项不再是旧文档里那种“对象模型声明有，真实验收没有”的伪闭环。
- 当前剩余边界主要在 producer/live consumer 的更大链路，不在 `HumanAssistTaskService` 本身的验收合同。

### 3.2 `/runtime-center/industry/{id}` 的 focus truth patching 已收紧到 canonical 边界

当前状态：`已修复`

本轮已确认：

- route 现在只接受 `assignment/backlog` 两类 canonical focus。
- `lane_id / cycle_id / report_id / 非 assignment/backlog 的 focus_kind` 会直接返回 `400`，不再在路由层伪造 focus truth。
- runtime views 允许展示“选中的 assignment/backlog 详情”，但不再借此伪造 `execution.current_focus_*`。

关键落点：

- `src/copaw/app/routers/runtime_center_routes_industry.py`
- `src/copaw/industry/service_runtime_views.py`
- `tests/industry/test_runtime_views_split.py`
- `tests/app/industry_api_parts/bootstrap_lifecycle.py`

真实性结论：

- 旧问题里“接口可以接受不存在的 lane/report/cycle 并直接注入 payload”的情况，本轮已不成立。
- 当前 focus contract 已明显比上一轮更真实、更收口。

### 3.3 `Runtime Center` 的 canonical surface-only 前端契约已补齐对应测试

当前状态：`已修复`

本轮新增发现并已修正：

- `console/src/pages/RuntimeCenter/useRuntimeCenter.ts` 当前已从 canonical `surface.cards` 派生 `businessAgents`，不再额外请求旧的 `requestRuntimeBusinessAgents()`。
- 相关 Vitest 仍在期待旧 endpoint，被本轮前端验证真实打出失败。
- 已将测试样例和断言改到现行 `surface-only` contract，上述 4 个前端测试现已全部通过。

关键落点：

- `console/src/pages/RuntimeCenter/useRuntimeCenter.test.ts`

真实性结论：

- 这不是生产实现回退，而是测试口径滞后。
- 当前前端 Runtime Center 至少在这条关键 contract 上与现实现状一致。

---

## 4. 部分收口但仍未完全闭环的问题

### 4.1 行业 bootstrap 的一部分 legacy dispatch 已收住，但 kickoff/auto-resume 还没完全切干净

当前状态：`部分修复`

本轮已确认的正向变化：

- public bootstrap 不再把 `auto_activate` 自动膨胀成 legacy goal dispatch。
- chat writeback 新建 schedule 不再扩张 industry instance 的 `schedule_ids` 真相。
- `run_operating_cycle(...)` 已把 task subgraph 传进 cycle/assignment planners。
- 本轮 `bootstrap_lifecycle.py` 已全量通过，因此这里不是“当前回归挂着”的 blocker。

仍然真实存在的问题：

- `service_activation.py`、`service_lifecycle.py` 里仍有大量 `goal_ids / schedule_ids / dispatch_goal_*` 残留路径。
- kickoff / auto-resume 仍没有完全改成 assignment/backlog-native execution。
- `IndustryInstanceRecord` 和 `StrategyMemoryRecord` 仍保留 legacy goal 相关运行事实。

结论：

- 当前是“功能回归已收住，架构债还在”。
- 不能把这一块写成“legacy goal/schedule 主链已经退役完成”。

### 4.2 acquisition decision 的路由回归已修，但 producer/kernel-task 一体化仍未完成

当前状态：`部分修复`

本轮已确认：

- `Runtime Center` approve/reject acquisition decision 现在优先走 canonical dispatcher。
- learning acquisition review-gate approve/reject 现在也优先走 canonical dispatcher。
- acquisition run 现在会把 confirm proposal 正式准入 kernel `waiting-confirm`，后续 approve/reject 会回写真实 kernel result。
- runtime-center actor route 对 sync/async dispatcher 都能兼容；当 kernel task 缺失时，现在会直接返回 `404`，不再偷偷走 acquisition 专属 route fallback。

关键落点：

- `src/copaw/app/routers/runtime_center_routes_actor.py`
- `src/copaw/app/routers/runtime_center_dependencies.py`
- `src/copaw/learning/acquisition_runtime.py`
- `tests/app/test_learning_api.py`
- `tests/app/test_runtime_center_api.py`

仍然真实存在的问题：

- `learning/acquisition_runtime.py` 内部仍保留 compatibility path：当 kernel dispatcher 不可用、或 kernel-backed task/decision 无法建立时，仍可能退回直接构造 `DecisionRequestRecord / TaskRecord`。
- 也就是说，route regressions 与 runtime-center 的长期 fallback 已经收住了，但 producer-side 和 kernel identity 还没有完全并成单一治理链。

结论：

- 当前可以写成“route-level regression 已修复，runtime-center actor route fallback 已删除”。
- 不能写成“DecisionRequest / learning acquisition 已完全统一到单一内核任务模型”。

### 4.3 文档高估口径这轮已纠偏，但不是一次性彻底收口

当前状态：`部分修复`

本轮会同步更新：

- `46问题文档.md`
- `TASK_STATUS.md`
- `docs/superpowers/plans/2026-04-06-closure-remediation-plan.md`

但仍然要诚实说明：

- 仓库里还有大量历史施工记录，本来就混合了“目标态描述、代码基线完成、focused harness 验证、真实 live smoke”。
- 不能假设改完这几个文档后，所有旧叙述都会自动变得严谨。

---

## 5. 当前仍然真实存在的高优先级问题

### 5.1 legacy `goal/schedule` 仍是行业主链里的平行事实源

真实问题：

- `IndustryInstanceRecord.goal_ids / schedule_ids`
- `StrategyMemoryRecord.active_goal_ids / active_goal_titles`
- lifecycle / activation 里的 `dispatch_goal_*`

这些事实仍然在表达同类运行状态。

结论：

- 当前不是“无 legacy”，只是“legacy 不再像上一轮那样到处引发直接回归”。

### 5.2 单一真相源依然没有完全建立

真实问题：

- industry instance
- strategy memory
- kernel live runtime
- route projection
- frontend local presentation

仍然有多层同时表达运行态。

结论：

- 当前可以说“对象模型已经比较成型”，不能说“单一真相源已经完成”。

### 5.3 capability execution 仍没有彻底收成单一执行前门

真实问题：

- 读模型收口明显优于执行模型。
- `Runtime Center /external-runtimes/actions` 现在已经改成 `kernel dispatcher submit -> execute_task`，并继承 mount `risk_level`，不再直接绕到 `capability_service.execute_task(...)`。
- 但 `tool / skill / MCP / query-time local delegate / react_agent builtin fallback / browser routine` 仍未完全统一到同一 admission/risk/evidence 前门。

结论：

- 这一块仍然是后续需要继续深挖的硬问题。

### 5.4 Decision / Evidence / Learning 仍不是一条完全统一的治理链

真实问题：

- acquisition 的 runtime-center route fallback 已删，但 learning producer 仍保留 compatibility fallback
- learning / patch / growth 与统一 `EvidenceRecord` 主链还不是完全同构
- 一些治理对象虽然已有正式 record，但 producer/front-door 仍分散

结论：

- 当前能说“比上一轮更一致”，不能说“已经单链治理”。

### 5.5 官方主入口更清晰了，但仍不能认定“唯一前门”

真实问题：

- `/runtime-center/chat/run` 仍然只是最明确的 operator-facing 前门。
- channel、cron、其他 runtime/action 入口仍然存在。

结论：

- 当前更像“主入口更明确”，不是“其他入口全部退役”。

---

## 6. 重复内容、逻辑不连贯与真实性边界

### 6.1 当前仍可观察到的重复真相源

- `IndustryInstanceRecord.goal_ids / schedule_ids`
- `StrategyMemoryRecord.active_goal_ids / active_goal_titles`
- kernel live runtime 内存态
- 各类 projection/read surface 的派生 focus/environment/owner

### 6.2 当前仍可观察到的逻辑不连贯点

- 行业 public bootstrap 已经不再自动 legacy dispatch，但 kickoff/auto-resume 还保留旧链。
- Runtime Center 的 focus query 已收成 assignment/backlog，但更大范围的 runtime truth 仍未统一到单一 owner。
- `Runtime Center /external-runtimes/actions` 已切回 dispatcher admission，但 query-time delegate / react_agent builtin fallback / system dispatch 仍存在额外绕路面。
- acquisition 的 runtime-center decision route 已经删除专属 fallback，但 learning producer 仍需要为非统一 kernel identity 兜底。

### 6.3 当前对“真实性”的正确表述

可以成立的说法：

- 关键后端主链现在有真实回归结果支撑。
- `bootstrap_lifecycle` 本轮全量通过。
- `HumanAssistTask` 的核心验收合同现在是真实现，不是伪强语义。
- `Runtime Center` 前后端主读面至少在本轮覆盖的 contract 上是连通的。

不能成立的说法：

- “项目已经完全闭环”
- “单一真相源已经完成”
- “所有治理入口已经收成单一主链”
- “legacy goal/schedule 已完全退役”

---

## 7. 最终定性

一句话定性：

> 当前 CoPaw 已经不是“概念型架构”或“假闭环”；它具备真实可跑、可测的正式主链。但它仍然是一个`关键回归已收住、若干核心债未切净`的部分闭环系统。

更准确的内部表述应该是：

- 正式主链：`能跑`
- 关键回归：`本轮已重新验证`
- HumanAssist 验收合同：`已修复`
- Runtime Center canonical focus query：`已修复`
- acquisition route regression：`已修复，runtime-center route fallback 已删，但 producer 统一未完成`
- external runtime capability front door：`已修复一条真实绕路，但更广泛 capability 前门仍未统一`
- legacy goal/schedule、单一真相源、单一治理链：`仍未完成`

---

## 8. 下一步优先顺序

建议继续按下面顺序推进：

1. 先切掉 kickoff/auto-resume 对 legacy `goal/schedule` 的主动依赖。
2. 把 acquisition proposal / decision producer 正式并进 kernel task identity，删掉 producer compatibility fallback 的长期合理性。
3. 继续删除 `goal_ids / schedule_ids / active_goal_ids` 这类平行真相源。
4. 继续收紧 capability execution 的统一 admission/risk/evidence 前门，重点清理 query-time delegate、react_agent builtin fallback、system dispatch。
5. 继续做文档真实性治理，把“目标态、代码基线、focused harness、默认回归、live smoke”分层写清。

这才符合当前仓库真正的问题顺序。

## 9. `2026-04-06` ???????????????????

??????????????????????????????????????

1. kickoff / auto-resume ? legacy `goal/schedule` ?????????
   - ?????`src/copaw/industry/service_lifecycle.py`?`src/copaw/industry/service_activation.py`?`src/copaw/industry/service_strategy.py`
   - ?????kickoff ???? assignment-native ???auto-resume / autonomy-status ??? legacy `goal/schedule` ?? execution-stage truth?bootstrap auto-learning ????? kickoff legacy goal dispatch ???
2. acquisition producer compatibility fallback ????kernel identity ?????
   - ?????`src/copaw/learning/acquisition_runtime.py`
   - ?????producer ???? free-form `DecisionRequestRecord / TaskRecord`??? kernel dispatcher-backed task store ???????????????? compatibility path?
3. capability execution ??????????
   - ?????`src/copaw/kernel/query_execution_runtime.py`?`src/copaw/agents/react_agent.py`?`src/copaw/capabilities/system_dispatch.py`
   - ?????query-time builtin tool delegate ?? child kernel task ?????`react_agent` delegate failure fail-closed?`system_dispatch` ?????? kernel admission????? turn failure?

??????????

- `python -m pytest tests/app/test_learning_api.py tests/app/test_runtime_center_api.py -q` -> `122 passed`
- `python -m pytest tests/agents/test_react_agent_tool_compat.py tests/kernel/test_query_execution_runtime.py tests/kernel/query_execution_environment_parts/dispatch.py tests/kernel/query_execution_environment_parts/lifecycle.py -q` -> `58 passed`
- `python -m pytest tests/app/test_capabilities_execution.py -k "system_dispatch_query or external_runtime or runtime_center" -q` -> `3 passed, 48 deselected`
- `python -m pytest tests/app/industry_api_parts/bootstrap_lifecycle.py::test_public_bootstrap_auto_activate_keeps_instance_active_without_legacy_goal_dispatch tests/app/industry_api_parts/bootstrap_lifecycle.py::test_chat_writeback_schedule_creation_does_not_expand_instance_schedule_truth tests/app/industry_api_parts/retirement_chain.py::test_industry_runtime_main_chain_exposes_live_assignment_chain_after_auto_activate -q` -> `3 passed`

????????????

- `goal_ids / schedule_ids / active_goal_ids` ??????????????????????legacy ????????????
- ??????????????????????????focused harness??????live smoke ??????

---

## 10. 2026-04-06 权威更新（覆盖前文同日 partial 记录）

当前权威结论：

- `kickoff / auto-resume` 已切到 assignment/backlog-native execution，不再依赖 legacy `goal/schedule` 作为运行前门真相。
- acquisition producer 已并入 kernel-backed identity；prediction / runtime-center / review-gate 的 capability lifecycle / decision 链现在都走统一 kernel/governed mutation 前门。
- capability execution 前门已收口：query-time builtin tool delegate、`react_agent` builtin fallback、capability-market install-template assignment、prediction capability retirement recommendation 都已并到统一治理入口。
- 物理删旧已完成：`IndustryInstanceRecord.goal_ids / schedule_ids`、`StrategyMemoryRecord.active_goal_ids / active_goal_titles`、`OperatingCycleRecord.goal_ids`、`WorkflowRunRecord.goal_ids / schedule_ids`、`ReportRecord.goal_ids` 已从 model / store schema / sqlite repository / runtime consumer 中移除。

真实性边界：

- 当前运行态、持久化 schema、repository 契约、主要读写链里，以上兼容字段已不再作为正式对象存在。
- 仍能搜索到的同名字面量，只剩两类：
  - `tests/state/test_state_store_migration.py` 里的旧 schema reset fixture，用来验证“旧库直接清空重建”的硬切策略。
  - `paused_schedule_ids` 这类 governance 字段，它们属于暂停控制面，不是被本轮删除的 parallel truth。

本轮最终验证证据：

- `python -m pytest tests/app/test_predictions_api.py tests/app/test_workflow_templates_api.py tests/kernel/query_execution_environment_parts/lifecycle.py tests/app/runtime_center_api_parts/overview_governance.py tests/industry/test_runtime_views_split.py tests/state/test_strategy_memory_service.py tests/state/test_sqlite_repositories.py tests/app/test_industry_service_wiring.py tests/state/test_main_brain_hard_cut.py tests/app/test_goals_api.py tests/app/test_runtime_chat_media.py tests/app/test_startup_recovery.py tests/app/industry_api_parts/bootstrap_lifecycle.py::test_public_bootstrap_auto_activate_keeps_instance_active_without_legacy_goal_dispatch tests/app/industry_api_parts/bootstrap_lifecycle.py::test_kickoff_execution_from_chat_dispatches_bootstrap_assignments_without_goal_dispatch tests/app/industry_api_parts/bootstrap_lifecycle.py::test_chat_writeback_schedule_creation_does_not_expand_instance_schedule_truth tests/app/industry_api_parts/runtime_updates.py::test_industry_list_instances_hides_empty_placeholder_records tests/app/industry_api_parts/runtime_updates.py::test_industry_list_instances_uses_lightweight_summary_without_detail_build tests/app/industry_api_parts/runtime_updates.py::test_industry_instance_status_reconciles_from_goal_states tests/app/industry_api_parts/runtime_updates.py::test_industry_instance_status_completes_with_static_team_membership_only tests/app/industry_api_parts/runtime_updates.py::test_industry_detail_backfills_execution_core_identity_with_delegation_first_defaults -q` -> `242 passed`

更新后的问题定性：

- 这轮不再把 `goal_ids / schedule_ids / active_goal_ids` 记为未完成尾巴。
- 后续若继续推进，只应按“进一步删掉历史 migration fixture 或重命名内部局部变量”这类低优先级整洁性任务登记，而不是继续把它们当成运行主链未闭环问题。
## 11. 2026-04-07 bootstrap draft-truth hardening 更新

- `IndustryInstanceRecord` 已新增正式 `draft_payload` 字段，并进入 SQLite schema / repository 持久化；bootstrap canonical draft 不再只存在于 request payload 或临时 compiler context。
- industry bootstrap 现在会把 canonicalized `IndustryDraftPlan` 原样写回实例内 `draft_payload`；strategy/runtime 读取行业草案时优先消费这份实例内真相，而不是再从 legacy goal/schedule 反推 bootstrap 草案。
- bootstrap backlog/cycle/assignment seed 已改为直接消费 canonical goal specs；`GoalRecord` 改为在 assignment-native seed 完成后按稳定 `goal_id` 物化，bootstrap write-path 不再依赖“先 materialize goal 再把 goal 当唯一 seed truth”的旧顺序。
- canonical bootstrap goal id 已稳定到 `team_id + goal slug`，这次顺带修掉了 retired-instance delete 场景下的跨实例 goal id 冲突。
- fresh process 还暴露出 `copaw.compiler.planning` package export 断层；`PlanningStrategicUncertainty / StrategyTriggerRule` 现已重新加入 package export，`IndustryService` 在 collect / 冷启动时不再因 planning re-export 缺项直接 `ImportError`。
- fresh verification:
  - `python -m pytest tests/state/test_state_store_migration.py::test_sqlite_state_store_initialize_upgrades_legacy_tables_before_schema_indexes tests/state/test_sqlite_repositories.py::test_sqlite_override_repositories_crud_round_trip tests/app/industry_api_parts/bootstrap_lifecycle.py::test_public_bootstrap_persists_draft_truth_and_uses_draft_goal_identity tests/app/industry_api_parts/retirement_chain.py::test_industry_delete_retired_instance_removes_persisted_runtime_state tests/app/industry_api_parts/runtime_updates.py::test_industry_chat_writeback_approved_staffing_proposal_unblocks_materialization -q` -> `5 passed`
  - `python -m pytest tests/app/industry_api_parts/bootstrap_lifecycle.py::test_kickoff_execution_from_chat_dispatches_bootstrap_assignments_without_goal_dispatch -vv -s` -> `1 passed in 864.29s`

## 12. 2026-04-07 bootstrap hard-cut 2A 更新

- `/industry/v1/bootstrap` 的正式公开 contract 已从 legacy `goals / schedules` 收口到 canonical `draft / backlog / cycle / assignments / schedule_summaries`；`routes` 里也不再暴露 legacy `goals` / `schedules` surface。
- bootstrap write-path 已继续收口：现在先以 canonical `goal_specs + schedule_specs` 起 `backlog/cycle/assignment`，随后才在下游物化 compatibility `GoalRecord / ScheduleRecord`。
- 因此，此前“industry bootstrap 仍会 materialize bootstrap goal/schedule，再喂给 backlog/cycle”的判断现已过时；它不再是当前 authoritative residual。
- 当前仍真实存在的 bootstrap 相关边界是：
  - compatibility `GoalRecord / GoalOverrideRecord` 仍保留为 leaf/detail artifact；
  - `ScheduleRecord` 仍保留为 cadence/runtime artifact；
  - 但它们都不再是 bootstrap 前门或 bootstrap seed 的 primary truth。
- 本次 fresh verification：
  - `python -m pytest tests/app/industry_api_parts/bootstrap_lifecycle.py -k "public_bootstrap_auto_activate_keeps_instance_active_without_legacy_goal_dispatch or public_bootstrap_auto_dispatch_materializes_assignment_tasks_without_goal_dispatch or public_bootstrap_persists_draft_truth_and_uses_draft_goal_identity or public_bootstrap_hard_cuts_legacy_goal_schedule_response_surface or public_bootstrap_seeds_backlog_from_canonical_schedule_specs" -q` -> `5 passed, 42 deselected`
  - `python -m pytest tests/app/industry_api_parts/retirement_chain.py::test_industry_delete_retired_instance_removes_persisted_runtime_state tests/app/industry_api_parts/retirement_chain.py::test_industry_delete_active_instance_clears_current_team tests/app/industry_api_parts/retirement_chain.py::test_industry_bootstrap_defaults_to_live_coordinating_contract -q` -> `3 passed`
  - `python -m pytest tests/app/industry_api_parts/runtime_updates.py::test_industry_bootstrap_goal_compile_regression_keeps_specialist_runtime_contract tests/app/industry_api_parts/runtime_updates.py::test_industry_bootstrap_specialist_runtime_metadata_only_persists_current_focus tests/app/industry_api_parts/runtime_updates.py::test_bootstrap_researcher_schedule_report_keeps_main_brain_continuity tests/app/industry_api_parts/runtime_updates.py::test_researcher_followup_assignment_persists_execution_core_continuity_without_backlog_anchor -q` -> `4 passed`

## 13. 2026-04-07 industry learning kickoff acquisition closure

- 此前 bootstrap / runtime-detail 审计里暴露出来的最后一条慢链，真实根因不是 runtime 主链未闭环，而是 `kickoff_execution_from_chat(...)` 默认会同步等待 `run_industry_acquisition_cycle(...)`，把 automatic acquisition discovery、install plan、onboarding run 一起塞进 kickoff 返回。
- 当前这条边界已经收口：
  - 默认 kickoff 不再同步阻塞 automatic acquisition cycle，而是改为在后台自动继续跑；
  - 只有显式传入 `include_learning_acquisition_cycle=True` 时，learning acquisition 才会同步加入本次 kickoff 返回；
  - industry automatic acquisition discovery 已默认限定 `providers=["install-template"]`，不再把 curated skill / MCP registry / remote provider 扫描当作同步 kickoff 的默认步骤。
- 这意味着：
  - assignment/backlog/cycle 的正式执行起链已和 acquisition exploration 慢链分离；
  - 系统仍会继续自动找 install-template / builtin runtime，不是把 acquisition 直接关掉；
  - install-template / builtin runtime 的自动 acquisition 仍可显式触发并保留；
  - SOP template 搜索仍保留在 discovery service 的独立分支，不受这次 provider 收口影响。
- 本次 fresh verification：
  - `python -m pytest tests/app/industry_api_parts/bootstrap_lifecycle.py::test_kickoff_execution_from_chat_does_not_block_on_learning_acquisition_cycle_by_default -q` -> `1 passed`
  - `python -m pytest tests/app/industry_api_parts/retirement_chain.py::test_industry_learning_kickoff_scopes_acquisition_discovery_to_install_templates -q` -> `1 passed`
  - `python -m pytest tests/app/industry_api_parts/retirement_chain.py::test_industry_learning_kickoff_materializes_acquisition_objects_and_exposes_them -q` -> `1 passed in 88.75s`
  - `python -m pytest tests/app/industry_api_parts/retirement_chain.py::test_industry_runtime_detail_and_goal_detail_use_formal_instance_store -q` -> `1 passed`

## 14. 2026-04-07 acquisition provider-scope correction

- 对上一节需要补一个重要纠偏：`providers=["install-template"]` 的收窄现在只属于 kickoff 管理的 automatic acquisition，不再是全局 acquisition 默认值。
- 当前真实行为已经调整为分层：
  - kickoff 默认后台 automatic acquisition 继续保持窄而快，只找 `install-template`，并继续保留 `sop_templates` 分支；
  - 显式 `/learning/acquisition/run` 在未指定 provider 时，已恢复 broad discovery default，不再被硬锁死为 `install-template`；
  - query/tool 前门 `discover_capabilities` 已恢复 `mcp-registry / mcp` provider 透传，query-time capability discovery 不再把 MCP 截断。
- 这次同时补了 background acquisition 的可见性：
  - `learning-acquisition.background-cycle-queued`
  - `learning-acquisition.background-cycle-completed`
  - `learning-acquisition.background-cycle-failed`
- 这意味着当前系统不会再出现“为了让 kickoff 变快，顺手把显式 acquisition 和 MCP discovery 也一起关窄”的同类错误。
- 本次 fresh verification：
  - `python -m pytest tests/app/test_learning_api.py::test_learning_api_acquisition_run_keeps_explicit_discovery_broad_by_default -q` -> `1 passed`
  - `python -m pytest tests/app/industry_api_parts/bootstrap_lifecycle.py::test_kickoff_execution_from_chat_does_not_block_on_learning_acquisition_cycle_by_default -q` -> `1 passed`
  - `python -m pytest tests/app/industry_api_parts/bootstrap_lifecycle.py::test_kickoff_execution_from_chat_publishes_background_acquisition_failure_event -q` -> `1 passed`
  - `python -m pytest tests/kernel/query_execution_environment_parts/dispatch.py::test_query_execution_service_discover_capabilities_preserves_mcp_registry_provider -q` -> `1 passed`
  - `python -m pytest tests/app/test_prediction_mcp_optimization_flow.py::test_missing_mcp_recommendation_executes_into_optimization_closure -q` -> `1 passed`
